# ROS-O launch configuration for Logitech QuickCams

Yes, they are old. Yes, they still work.

## v4l2 configuration

I recommend pyvidctrl for v4l2-settings

```
virtualenv venv
source venv/bin/activate
pip install git+https://github.com/antmicro/pyvidctrl
pyvidctrl
```
